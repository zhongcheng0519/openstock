---
globs: frontend/*
alwaysApply: false
---
### 核心架构
- 使用 Vue 3 Composition API + `<script setup>` + TypeScript
- 响应式优先 `ref()`，大型对象集合用 `reactive()`

### 组件规范
- 文件命名 PascalCase，模板引用同样使用 PascalCase
- Props: `defineProps<{...}>()` + `withDefaults`
- Events: `defineEmits<{...}>()`

### Composables
- 逻辑超过 50 行或可复用时，抽离到 `src/composables/useXxx.ts`
- 返回明确类型或 `Refs`

### 状态管理与路由
- 状态管理: Pinia (Setup Store 风格)，禁止 Vuex
- 路由: Vue Router 4 + 动态导入懒加载

### 模板与样式
- 模板禁止复杂表达式，逻辑封装在 `computed`
- 样式使用 `<style scoped>`，优先 Tailwind CSS
- `v-for` 必须提供唯一 `:key`

### 开发习惯
- DRY 原则，语义化命名代替注释
- 异步操作必须 `try-catch` + 用户提示
